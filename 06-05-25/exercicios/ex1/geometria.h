#ifndef GEOMETRIA_H
#define GEOMETRIA_H

// Triângulo
float area_triangulo(float base, float altura);
float perimetro_triangulo_gen(float a, float b, float c); // Triângulo geral (não equilátero)

// Círculo
float area_circulo(float raio);
float perimetro_circulo(float raio);

// Retângulo
float area_retangulo(float base, float altura);
float perimetro_retangulo(float base, float altura);
#endif
